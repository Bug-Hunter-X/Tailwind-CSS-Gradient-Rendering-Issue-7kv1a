```javascript
<div class="bg-gradient-to-r from-blue-500 to-purple-500 p-4 rounded-lg">
  <!-- Content here -->
</div>
```
This code uses Tailwind's gradient feature.  However, if you're using an older version of Tailwind or have a conflicting configuration, the gradient might not render correctly.  This could manifest as a solid color instead of a gradient or no styling at all.